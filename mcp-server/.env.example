# Base URL of the backend API (backend/.env's PORT).
BACKEND_URL=http://localhost:3001

# Must match the backend's INTERNAL_API_KEY exactly — required for every
# add/update/delete tool call (the backend rejects writes without it).
INTERNAL_API_KEY=
