import styles from './DashboardPlaceholder.module.css';

// Content for this section isn't defined yet — deliberately empty until
// the owner specifies what belongs on the Dashboard.
function DashboardPlaceholder() {
  return <p className={styles.placeholder}>Nothing here yet.</p>;
}

export default DashboardPlaceholder;
