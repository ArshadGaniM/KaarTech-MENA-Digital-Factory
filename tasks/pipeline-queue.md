# Pipeline Queue

> Living queue of features — queued/in_flight/completed/halted/error, plus the
> current session's active run ID. Read this FIRST every time before deciding
> whether to resume or start fresh (CLAUDE.md §7.7).

**Active run ID:** none — direct-build is the current standing mode, with the "continuous autonomous execution through the queue" standing instruction (§7.2) in effect: once a feature's gate passes and pushes, immediately pick up the next queued/in_flight feature without waiting to be told. Queue is now empty as of FEAT-14's completion — no further work queued. Also this session: found and fixed a real production gap — the Render backend's `autoDeploy` had silently stopped firing after PR #13 (2026-09-16), so every feature merged since (FEAT-5 through FEAT-12) never reached production despite the Vercel frontend being current. Triggered a manual deploy (`dep-dao4080ae00c73amau5g`) to the latest `main` commit; confirmed live via curl (`/v1/schema/entity-relationships` responding) and a full headless-browser walkthrough of the real site (marketing page, Dashboard, all 12 tables, Entity Relationship — no errors, empty tables correctly showing "No records yet"). A scheduled Routine ("KaarTech Digital Factory — Dev Pipeline Continuation", `trig_012guAqCcCZ6669t4pQAn5bC`, hourly) exists per the owner's standing instruction to keep executing queued features across session limits (CLAUDE.md §7.3) — **known limitation: it holds no MCP connectors**, so it can't reach Supabase/GitHub tools until recreated from a session or the claude.ai UI that holds those grants.

**Feature counter:** 15

## Queue

| ID | Status | Requirement | Branch |
|---|---|---|---|
| FEAT-15 | in_flight | Delete `<Entity>` popup (type a Code/ID, matched against active rows, deletes on a match) plus a per-row Delete action, on every master-data table page — both soft-delete via the existing backend DELETE route. Gate passed (WARN, zero FAIL/Critical, security PASS clean); a squash-divergence repair merge is bundled into this push to re-trigger the auto-PR workflow's freshness check after two earlier pushes (one touching only the gate report, one touching neither file) failed it. | claude/trusting-curie-hlx1r6 |

## Completed

| ID | Status | Requirement | Branch | Architectural decision |
|---|---|---|---|---|
| FEAT-14 | completed | Add an "Add [Entity]" button to the top of every master-data table's page (Add Resource, Add Project, Add Project Assignment, etc.), opening a pop-up form generically built from that table's real field descriptor (self-updating, same principle as FEAT-12's Entity Relationship page — not 12 hand-built forms): auto-generated fields (hasCode) excluded entirely, FK fields rendered as a pick-list dropdown populated from the referenced table's live records, plain manual fields rendered as a normal input matching their type, read-only derived/lookup fields excluded. An "Add" button in the pop-up submits, creates the record, closes the pop-up, and refreshes the table. | claude/trusting-curie-hlx1r6 | Built directly. New `fieldsOf()` in `backend/src/entityRelationships.js` extends the existing self-updating `GET /v1/schema/entity-relationships` endpoint (FEAT-12) with a `fields` array per table (key/type/required/maxLength/values/resolved-reference-route), reusing the same "derive everything from `MASTER_DATA_TABLES`, nothing hand-maintained" principle rather than building 12 separate forms. New generic `AddRecordModal` component drives entirely off that descriptor — no per-table branching — rendering a text/number/date input, an enum `<select>`, or a reference pick-list per field shape; pick-lists exclude soft-deleted rows and use a best-effort `row.name ?? row.projectName` label heuristic. `useMasterDataTable` gained a `refetch()` (bumps a `refreshToken` dependency) so the table reloads after a successful create. `MasterDataTable` now always renders a toolbar with an "Add {singularLabel}" button. Accepted tradeoff, disclosed and logged (CLAUDE.md §20, `.env.example`): the new `createRecord()` sends `VITE_INTERNAL_API_KEY` as a header from the browser — Vite inlines `VITE_` vars into the built bundle, so this key is visible to any site visitor and is no longer a real secret once set. Accepted because the app has no login/auth system yet (same category as other pre-auth tradeoffs already logged). Owner must set this var in Vercel themselves (I don't have permission — `filter_project_envs` returned 403). Gate: 8/8 agents ran, verdict WARN (zero FAIL, zero Critical, security PASS clean) — merged per CLAUDE.md §9.5. test-writer and doc-writer applied direct fixes: added dedicated `useMasterDataTable` refetch test coverage (previously zero), a date-input render test, a real-table `fieldsOf()` regression test with no `references` field, strengthened Cancel-button coverage, and a `backend/README.md` cross-reference from the per-table field docs to the new live `fields` endpoint. Non-blocking follow-ups logged in the gate report: switch the pick-list fetch from `Promise.all` to `Promise.allSettled` (one failed reference-table fetch currently blanks every FK field's options, not just the failed one), add a test for the generic non-422 submission-failure banner, and surface a visible error if a future table's FK ever fails to resolve a route (not reachable with the current 12 tables). Also this session: diagnosed and fixed a real, previously-unnoticed production incident — Render's `autoDeploy` had silently stopped firing for ~5 days across 15+ merges (see header note above). Live browser verification of the actual Add-record submit flow is deferred until the owner sets `VITE_INTERNAL_API_KEY` in Vercel. |
| FEAT-12 | completed | Add a new sidebar section, "Entity Relationship", explaining how every master-data table connects to the others: each entity's own identity field (auto-generated `code` vs. caller-supplied unique key vs. none), and — for every enforced FK — which table it references and which fields it live-looks-up from that reference (including the transitive Teams→Departments hop). Must self-update as the schema evolves. | claude/trusting-curie-hlx1r6 | Built directly. New `backend/src/entityRelationships.js` derives everything from `MASTER_DATA_TABLES` (pure functions: `identityOf`/`relationshipsOf`/`lookupsOf`), exposed via a new unauthenticated `GET /v1/schema/entity-relationships` (matches every other GET route's no-auth convention). Added a new optional `identityField` metadata property to `resources`/`projects` (pure metadata, not consumed by the write path) to distinguish "caller-supplied-unique" identity from `hasCode`'s "auto-generated" and the fallback "none" (used by `resource_cost`/`resource_deployment`/`project_assignments`, all now explicitly commented as deliberate). Frontend: new `EntityRelationship` component/hook/fetch helper, appended as the LAST `NAV_ITEMS` entry (not inserted after Dashboard) specifically to avoid shifting any real table's array index that `AppShell.test.jsx`/`navigation.test.js` rely on; `AppShell.jsx` now branches on a new `kind` field instead of a route-truthiness ternary. Verified end-to-end in a real browser (backend+frontend dev servers, headless Chromium dump-dom) — confirmed the chained lookup and "no identity" cases render correctly with live data. Gate: 8/8 agents PASS after fixing 3 WARNs — doc-writer found the README's example response was missing 3 of 5 real `lookups` entries; test-writer found no HTTP-layer test existed for the new route (only the pure function was tested) and no test covered the label-fallback branch, both added; silent-failure-hunter found the "none" identity fallback and the label-fallback path both silently degraded with no signal distinguishing "deliberate" from "someone forgot" — fixed with explanatory comments and a `console.warn`. No Critical findings. Queue is now empty — no further work queued as of this feature's completion. |
|---|---|---|---|---|
| FEAT-11 | completed | Add a brand-new "Project Assignments" master-data table with: `projectId` (required, pick-list only — FK-validated against `projects.project_id`), `projectName`/`projectProfitCenterCode` (read-only, live-looked-up), `teamId` (required, pick-list only, FK-validated against `teams.code`), `teamName` (read-only, live-looked-up), `departmentId`/`departmentName` (read-only, live-looked-up **transitively** through the selected Team's own department link), `projectAssignmentStartDate`/`projectAssignmentEndDate` (both manually entered; end must not be earlier than start, enforced). | claude/trusting-curie-hlx1r6 | Built directly. Required TWO new generic framework capabilities in `masterDataSchema.js`, both first-of-their-kind: (1) chained/transitive lookups (`lookup.via`) — `buildLookupPlan` now resolves a lookup's join source to an *earlier* lookup's own alias instead of the base table when `via` is set, so `departmentId`/`departmentName` join off the `teams` lookup's alias (`lookup_1.department_code`), not a column on `project_assignments` itself; (2) cross-field validation (`table.crossFieldValidations`, a `"dateRange"` rule type) via a new `validateCrossFields()`, wired into both POST and PATCH in `masterDataRouter.js` — a PATCH sending only one date is checked against the other's current, stored value. Also added a `"date"` field type (`validateBody`, mcp-server's `fieldSchema` via `z.string().date()`). Migration 0018 applied live to Supabase: two FK constraints (second table after `resource_deployment` with two at once) plus a DB-layer `CHECK` constraint mirroring the app-layer date-range rule as defense-in-depth. Gate: 8/8 agents PASS after fixing 2 WARNs — doc-writer found `mcp-server/README.md`'s stale tool/table counts and a missing index-name citation in `backend/README.md`'s migration row; test-writer found `masterDataSchema.js` had no direct unit tests for `via`/`validateCrossFields` (only exercised indirectly through the router) and that the FK-violation-race test only covered one of the two constraints — both fixed. code-reviewer flagged one non-blocking robustness note (`via` resolves to the *first* matching table name, not "nearest earlier entry" — not exploitable by today's config). No Critical findings. Also this session: a scheduled Routine was created for cross-session-limit pipeline continuation (see this file's header note) — created with no MCP connectors attached (disclosed to owner), a known limitation until recreated from a session/UI holding those grants. |
|---|---|---|---|---|
| FEAT-13 | completed | Verify "Marked Deleted" is a standing invariant across every master-data table: a new record defaults to `markedDeleted: "No"`; deleting a row never physically removes it — only sets `deleted_at`, flipping `markedDeleted` to `"Yes"`. Owner asked this be logged as a feature. | claude/trusting-curie-hlx1r6 | No code change — verified directly against `backend/src/masterDataSchema.js:61` (`response.markedDeleted = row.deleted_at != null ? "Yes" : "No"`), `backend/src/masterDataRouter.js`'s DELETE handler (`update ... set deleted_at = now() ... returning id`, never a physical `delete from`), and `src/lib/masterDataApi.js`'s `AUDIT_COLUMNS` (`{ key: "markedDeleted", label: "Marked Deleted" }`, spread into every one of the 11 tables' column lists, including the just-added `projects`). This is baked into the generic framework, not per-table code, so it already covers every current table and will automatically cover every future one built on this same descriptor pattern — nothing to build. |
| FEAT-10 | completed | Add a brand-new "Projects" master-data table (not one of the original 9 — a new sidebar section, new backend route/table/MCP tools) with: `projectId` (manually entered by the user, **not** auto-generated — no `hasCode` pattern here, unlike Practices/Departments/Teams/Positions), `projectName` (manually entered), `projectProfitCenterCode` (manually entered, plain string — no FK validation specified, no lookup table named). Plus standard audit columns. | claude/trusting-curie-hlx1r6 | Built directly (established pivot since FEAT-7). First table since FEAT-5 with **zero enforced FK relationships** — no `references`/`lookups` on any field, no `hasCode` trigger. `projectId` follows the `resources.employeeId` precedent (migration 0012): caller-supplied, DB-enforced-unique (`projects_project_id_unique`), mapped through the existing `isUniqueViolation()`/`duplicateFieldError()` path rather than a new one. Migration 0017 applied live to Supabase, confirmed via `mcp__Supabase__list_tables`. Verified the generic framework degrades correctly for a zero-FK/zero-lookup table: `validateReferences()`'s field filter naturally produces an empty `fieldsToCheck` (no vacuous-truth bug), and `buildLookupPlan`'s `table.lookups ?? []` handles the key being entirely absent. New `masterDataRouter.projects.test.js` is the first dedicated test file exercising the unique-violation (23505) path end to end, on both POST and PATCH. Gate: 8/8 agents PASS after fixing 2 WARNs — test-writer found and fixed a missing PATCH-side unique-violation test (`projectId` participates in PATCH's SET clause exactly like POST's INSERT, unlike `code`-owned fields on other tables); doc-writer found and fixed a stale route count in `backend/README.md` ("ten routes" → "eleven"). No Critical findings. |
| FEAT-7 | completed | Build out the Resource Deployment table's real schema: `employeeId` (required, must reference an existing Resources row, enforced), `employeeName` (read-only, live-looked-up from the referenced resource), `positionId` (required, must reference an existing Positions row by its own auto-generated code, enforced), `positionName` (read-only, live-looked-up from the referenced position). Plus standard audit columns (already present). | claude/trusting-curie-hlx1r6 | First table built **directly**, not via the Workflow pipeline — the `engineer-mvp` stage hit a third consecutive session-limit failure (after FEAT-8's `developer` and FEAT-9's `frontend-engineer` stages), and the owner explicitly pivoted feature-building away from the Workflow tool from this feature forward. Verified the pipeline's partial `codebase-explorer`/`business-analyst`/`engineer-mvp` output was sound, then completed the build by hand following the same patterns. First table with **two concurrent `references` fields and two concurrent `lookups` entries at once** (employeeId→resources.employee_id, positionId→positions.code) — `validateReferences()`'s existing `Promise.all` concurrency (added during FEAT-5 ahead of this exact need) and the generic `lookups` framework were reused verbatim, zero new logic in `masterDataSchema.js`/`masterDataRouter.js`. Migration 0016 applied live to Supabase: drops the placeholder `name` column, adds `employee_id`/`position_code` with two FK constraints (`fk_resource_deployment_resources`, `fk_resource_deployment_positions`) and two indexes — confirmed on the deployed schema via `mcp__Supabase__list_tables`. New `masterDataRouter.resourceDeployment.test.js` (274 lines) is the first suite to test two-FK interaction/independence explicitly (both-invalid-together, one-bad-one-valid, independent lookup nulling, race-condition constraint-name disambiguation). Gate: 8/8 agents PASS after fixing 2 gaps — test-writer's missing mcp-server descriptor tests (self-fixed by the agent, committed), and doc-writer's 2 stale-docs FAILs (missing 0016 migration row in `backend/README.md`; `mcp-server/README.md`'s 4th-consecutive-feature recurrence of grouping `resource_deployment` under a stale placeholder row — both fixed directly). No Critical findings. This same push also lands the project-wide model-ceiling standing policy (CLAUDE.md §2). |
| FEAT-9 | completed | Add a brand-new "Positions" master-data table (not one of the original 9 — a new sidebar section, new backend route/table/MCP tools) with: `code` (auto-generated business ID, immutable), `name` (manually entered, editable anytime), `teamCode` (required, must reference an existing Teams row by its own auto-generated code, enforced), `teamName` (read-only, live-looked-up from the referenced team). Plus standard audit columns. | claude/trusting-curie-hlx1r6 | Round 1's `frontend-engineer` stage hit a session/token limit mid-run (not an architecture halt or code defect) — resumed from cache per CLAUDE.md §7.3, completed clean on retry. Migration 0015 applied live to Supabase: a brand-new table created in one migration (unlike FEAT-5/FEAT-8's placeholder-upgrade migrations), combining the base master-data shape + `hasCode` sequence/trigger (`POS-001` style) + `team_code` FK to `teams.code` (`fk_positions_teams`) + index, since the table starts empty. Reused FEAT-5/FEAT-8's generic `validateReferences()`/`lookups` framework verbatim — third table on this pattern, zero new logic in `masterDataSchema.js`/`masterDataRouter.js`. New `masterDataRouter.positions.test.js` correctly used the shared `masterDataRouter.testHelpers.js` harness (extracted during FEAT-8's gate) rather than duplicating boilerplate a third time. Final Enterprise Architect sign-off independently re-verified every touched file, re-ran all three test suites (108/108 backend, 13/13 mcp-server, 16/16 positions-specific), and queried live Supabase directly to confirm the FK constraint actually exists on the deployed schema. Verdict: PASS. |
| FEAT-8 | completed | Build out the Teams table's real schema: `code` (auto-generated business ID, immutable), `name` (manually entered, editable anytime), `departmentCode` (required, must reference an existing Departments row by its own auto-generated code, enforced), `departmentName` (read-only, live-looked-up from the referenced department). Plus standard audit columns. | claude/trusting-curie-hlx1r6 | Round 1 hit a hard error (developer stage exceeded the StructuredOutput retry cap) — not an architecture halt, resumed from cache per CLAUDE.md §7.3 and completed clean on retry. Migration 0014 applied live to Supabase: `code`/sequence/trigger (TEAM- prefix, same pattern as departments/practices), `department_code` with a real FK to `departments(code)` (`fk_teams_departments`) + index. Reused FEAT-5's generic `validateReferences()`/`lookups` framework verbatim — zero new logic in `masterDataSchema.js`/`masterDataRouter.js`, proving the framework generalizes to a second FK relationship. Final Enterprise Architect sign-off independently re-verified every touched file, re-ran both test suites (92/92 backend, 13/13 mcp-server), confirmed no new coupling. Verdict: PASS. |
| FEAT-5 | completed | Build out the Resource Cost table's real schema: `employeeId` (required, must reference an existing Resources row, enforced), `employeeName` and `employeeDesignation` (read-only, live-looked-up from the referenced resource — not stored/editable columns), `offshoreCost` and `onsiteCost` (both optional numeric). Plus standard audit columns (already present). | claude/trusting-curie-hlx1r6 | Round 1 halted at architecture-critic (shared list-query `ORDER BY ... name asc` would break once resource_cost's `name` column is dropped) — resumed round 2 with a per-table `sortColumn` fix and a soft-delete filter added to the employee join. Introduced two new generic master-data framework capabilities, reusable by later tables: `validateReferences()` (opt-in per-field FK-existence check against a live query) and a `lookups` descriptor driving a LEFT JOIN for read-only derived fields, both wired into the shared `masterDataRouter.js`/`masterDataSchema.js`. Migration 0013 applied live to Supabase: adds `employee_id`/`offshore_cost`/`onsite_cost`, drops the placeholder `name` column, adds `fk_resource_cost_resources` FK constraint + index. Final Enterprise Architect sign-off (stage 9) independently re-verified — not on prior-stage trust — that all 9 touched/test files exist with real content, re-ran `node --test` (73/73 backend, 0 failures) and `npm test` (7/7 mcp-server), re-ran oxlint (clean), and directly queried live Supabase schema/constraints to confirm the deployed table matches the descriptor exactly. No stubs, no TODOs, no debug code. Verdict: PASS, ready for §9.1 auto-merge-to-main. |
| FEAT-6 | completed | Prefix every Resources table column label (except Employee ID) with "Employee " (e.g. "Org Chart" → "Employee Org Chart"). | claude/trusting-curie-hlx1r6 | Built directly — trivial single-file display-label rename (CLAUDE.md §2 "skip planning for renames"), skipped the full 8-agent gate in favor of one `/code-review` pass (no findings) since the diff has no behavioral/structural surface. Field keys, DB columns, and MCP tool parameter descriptions untouched — display labels only, in `src/lib/masterDataApi.js`. |
| FEAT-4 | completed | Add "Dashboard" as its own standalone sidebar section, placed first in the nav list. Content deliberately empty for now. | claude/trusting-curie-hlx1r6 | Built directly. `NAV_ITEMS[0]` is now a synthetic `{id: 'dashboard', route: null, columns: null}` entry ahead of the master-data-table-derived items; `AppShell` branches on `activeItem.route` to render a new `DashboardPlaceholder` component instead of `MasterDataTable` for it. Since Header's "Dashboard" link and Hero's "Enter the Application" CTA both used `NAV_ITEMS[0].hash`, they now correctly land on Dashboard by default. Gate: 1 important test-coverage gap (pr-test-analyzer) fixed with an `App.test.jsx` integration test tying the actual hash value to the real rendered outcome; 1 doc gap (doc-writer) fixed. 35/35 frontend + 50/50 backend tests pass. |
| FEAT-3 | completed | Add a primary "Enter the Application" CTA button to the marketing landing page's Hero section, entering the dashboard. Landing page otherwise unchanged. | claude/trusting-curie-hlx1r6 | Built directly. While wiring the button (linking to `NAV_ITEMS[0].hash`), found and fixed a real, already-live bug from FEAT-2: the marketing page's "Delivery Centers" anchor (`#delivery-centers`) collided with the dashboard's own Delivery Centers table route, since `APP_SHELL_HASHES` also treats that hash as an app-shell hash — clicking it (in both Header and Hero) silently entered the dashboard instead of scrolling to the marketing section. Renamed the marketing anchor to `#our-delivery-centers`. Gate: 1 FAIL (test-writer) + 1 Critical (pr-test-analyzer), both the same finding (no regression test on the collision fix) confirmed by 4 agents total — fixed with tests asserting the anchor is absent from `APP_SHELL_HASHES`, guarding the whole class of bug. 32/32 frontend + 50/50 backend tests pass. |
| FEAT-2 | completed | Rework the AppShell sidebar/dashboard from FEAT-1: list each master-data table as its own separate sidebar item instead of one generic entry; clicking an item shows that table's data on the right. Remove "Master Data" from user-facing UI text. Top page header reads "Dashboard". | claude/trusting-curie-hlx1r6 | Built directly (owner chose not to run the full dev-team pipeline for this well-scoped, concrete change). `NAV_ITEMS` is now derived straight from `MASTER_DATA_TABLES` (one nav item per table, carrying `route`/`columns`) instead of holding a single generic entry with an inner tab-bar view — removed the now-unused `MasterDataView` component. `AppShell` renders `MasterDataTable` directly per selected item; `TopBar` shows a static "Dashboard" heading (sidebar's active-item state carries which section is selected). Marketing `Header`'s nav link relabeled "Dashboard", pointing at the first table's hash. 26/26 frontend tests pass, 50/50 backend tests pass, clean build/lint, self-reviewed via `/code-review` (no findings). |
| FEAT-1 | completed | Redesign the frontend UI so it reads as a multi-user internal application (dashboard/app shell — nav, workspace chrome, data-dense views) rather than a public-facing "portal" landing-page style. | claude/trusting-curie-hlx1r6 | Round-1 architecture-critic halt (AC-1: AppShell wrapped in old marketing Header/Footer) resolved by solution-architect's round-2 SDD amendment: App.jsx now uses a single outermost `APP_SHELL_HASHES.includes(hash)` ternary so AppShell and the marketing Header/main/Footer tree never co-mount. New: AppShell/Sidebar/TopBar/UserBadge components, src/lib/navigation.js, src/lib/currentUser.js (mock, explicitly labeled — no real auth backend exists), src/hooks/useCurrentUser.js. MasterDataTable re-parented as a child view inside AppShell. Final enterprise-architect-post sign-off: PASS — clean `oxlint`, clean `npm run build` (58 modules), 25/25 tests passing, no stubs/TODOs/secrets, no unresolved findings. Ready for CLAUDE.md §9.1 auto-merge-to-main. |
