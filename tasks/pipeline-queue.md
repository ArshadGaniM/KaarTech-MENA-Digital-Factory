# Pipeline Queue

> Living queue of features — queued/in_flight/completed/halted/error, plus the
> current session's active run ID. Read this FIRST every time before deciding
> whether to resume or start fresh (CLAUDE.md §7.7).

**Active run ID:** (launching FEAT-8 now)

**Feature counter:** 11

## Queue

| ID | Status | Requirement | Branch |
|---|---|---|---|
| FEAT-8 | in_flight | Build out the Teams table's real schema: `code` (auto-generated business ID, immutable — same `hasCode` pattern as Practices/Delivery Centers/Departments/Modules, e.g. `TEAM-001`), `name` (manually entered, editable anytime), `departmentCode` (required, must reference an existing Departments row by its own auto-generated code, enforced), `departmentName` (read-only, live-looked-up from the referenced department). Plus standard audit columns (already present). Confirmed execution order: FEAT-5 (completed) → FEAT-8 → FEAT-9 → FEAT-7 → FEAT-10 → FEAT-11. Reuses the generic FK-validation (`validateReferences`) + live-lookup (`lookups`) framework FEAT-5 built — first table besides resource_cost to use either. | claude/trusting-curie-hlx1r6 |
| FEAT-9 | queued | Add a brand-new "Positions" master-data table (not one of the original 9 — a new sidebar section, new backend route/table/MCP tools) with: `code` (auto-generated business ID, immutable — `hasCode` pattern, e.g. `POS-001`), `name` (manually entered, editable anytime), `teamCode` (required, must reference an existing Teams row by its own auto-generated code, enforced), `teamName` (read-only, live-looked-up from the referenced team). Plus standard audit columns. Confirmed execution order: FEAT-5 → FEAT-8 → FEAT-9 → FEAT-7 → FEAT-10. Launch once FEAT-8 reaches completed/halted, since Team ID validation needs Teams to actually exist first. | claude/trusting-curie-hlx1r6 |
| FEAT-7 | queued | Build out the Resource Deployment table's real schema: `employeeId` (required, must reference an existing Resources row, enforced — same pattern as FEAT-5), `employeeName` (read-only, live-looked-up from the referenced resource), `positionId` (required, must reference an existing Positions row by its own auto-generated code, enforced — upgraded from the originally-planned unvalidated/manual placeholder now that FEAT-9 gives Positions a real code to validate against), `positionName` (read-only, live-looked-up from the referenced position). Plus standard audit columns (already present). Confirmed execution order: FEAT-5 → FEAT-8 → FEAT-9 → FEAT-7 → FEAT-10. Launch once FEAT-9 reaches completed/halted. | claude/trusting-curie-hlx1r6 |
| FEAT-10 | queued | Add a brand-new "Projects" master-data table (not one of the original 9 — a new sidebar section, new backend route/table/MCP tools) with: `projectId` (manually entered by the user, **not** auto-generated — no `hasCode` pattern here, unlike Practices/Departments/Teams/Positions), `projectName` (manually entered), `projectProfitCenterCode` (manually entered, plain string — no FK validation specified, no lookup table named). Plus standard audit columns. Owner explicitly confirmed all three business fields are user-supplied, nothing auto-generated. Confirmed execution order: FEAT-5 (running) → FEAT-8 → FEAT-9 → FEAT-7 → FEAT-10 → FEAT-11. Launched once FEAT-7 reaches completed/halted. | claude/trusting-curie-hlx1r6 |
| FEAT-11 | queued | Add a brand-new "Project Assignments" master-data table (not one of the original 9 — new sidebar section, backend route/table/MCP tools) with: `projectId` (required, pick-list only — **no manual entry allowed** — FK-validated against `projects.project_id`), `projectName` and `projectProfitCenterCode` (read-only, live-looked-up from the selected Project), `teamId` (required, pick-list only, FK-validated against `teams.code`), `teamName` (read-only, live-looked-up from the selected Team), `departmentId` and `departmentName` (read-only, live-looked-up **transitively** through the selected Team's own department link — Project Assignments → Teams → Departments, not a direct column/single-hop join on this table), `projectAssignmentStartDate` and `projectAssignmentEndDate` (both manually entered — field/label is "Project Assignment Start/End Date", not plain "Start/End Date"; `projectAssignmentEndDate` must not be earlier than `projectAssignmentStartDate`, enforced). Plus standard audit columns. Requires two new generic framework capabilities beyond what FEAT-5 built: a chained/transitive lookup (join through an intermediate table, not just directly-referenced-table joins) and cross-field validation (endDate >= startDate) — flagged for architecture-critic's explicit review given they're new capability classes, not just another table using the existing framework. Sequenced last, launched once FEAT-10 reaches completed/halted. | claude/trusting-curie-hlx1r6 |

## Completed

| ID | Status | Requirement | Branch | Architectural decision |
|---|---|---|---|---|
| FEAT-5 | completed | Build out the Resource Cost table's real schema: `employeeId` (required, must reference an existing Resources row, enforced), `employeeName` and `employeeDesignation` (read-only, live-looked-up from the referenced resource — not stored/editable columns), `offshoreCost` and `onsiteCost` (both optional numeric). Plus standard audit columns (already present). | claude/trusting-curie-hlx1r6 | Round 1 halted at architecture-critic (shared list-query `ORDER BY ... name asc` would break once resource_cost's `name` column is dropped) — resumed round 2 with a per-table `sortColumn` fix and a soft-delete filter added to the employee join. Introduced two new generic master-data framework capabilities, reusable by later tables: `validateReferences()` (opt-in per-field FK-existence check against a live query) and a `lookups` descriptor driving a LEFT JOIN for read-only derived fields, both wired into the shared `masterDataRouter.js`/`masterDataSchema.js`. Migration 0013 applied live to Supabase: adds `employee_id`/`offshore_cost`/`onsite_cost`, drops the placeholder `name` column, adds `fk_resource_cost_resources` FK constraint + index. Final Enterprise Architect sign-off (stage 9) independently re-verified — not on prior-stage trust — that all 9 touched/test files exist with real content, re-ran `node --test` (73/73 backend, 0 failures) and `npm test` (7/7 mcp-server), re-ran oxlint (clean), and directly queried live Supabase schema/constraints to confirm the deployed table matches the descriptor exactly. No stubs, no TODOs, no debug code. Verdict: PASS, ready for §9.1 auto-merge-to-main. |
| FEAT-6 | completed | Prefix every Resources table column label (except Employee ID) with "Employee " (e.g. "Org Chart" → "Employee Org Chart"). | claude/trusting-curie-hlx1r6 | Built directly — trivial single-file display-label rename (CLAUDE.md §2 "skip planning for renames"), skipped the full 8-agent gate in favor of one `/code-review` pass (no findings) since the diff has no behavioral/structural surface. Field keys, DB columns, and MCP tool parameter descriptions untouched — display labels only, in `src/lib/masterDataApi.js`. |
| FEAT-4 | completed | Add "Dashboard" as its own standalone sidebar section, placed first in the nav list. Content deliberately empty for now. | claude/trusting-curie-hlx1r6 | Built directly. `NAV_ITEMS[0]` is now a synthetic `{id: 'dashboard', route: null, columns: null}` entry ahead of the master-data-table-derived items; `AppShell` branches on `activeItem.route` to render a new `DashboardPlaceholder` component instead of `MasterDataTable` for it. Since Header's "Dashboard" link and Hero's "Enter the Application" CTA both used `NAV_ITEMS[0].hash`, they now correctly land on Dashboard by default. Gate: 1 important test-coverage gap (pr-test-analyzer) fixed with an `App.test.jsx` integration test tying the actual hash value to the real rendered outcome; 1 doc gap (doc-writer) fixed. 35/35 frontend + 50/50 backend tests pass. |
| FEAT-3 | completed | Add a primary "Enter the Application" CTA button to the marketing landing page's Hero section, entering the dashboard. Landing page otherwise unchanged. | claude/trusting-curie-hlx1r6 | Built directly. While wiring the button (linking to `NAV_ITEMS[0].hash`), found and fixed a real, already-live bug from FEAT-2: the marketing page's "Delivery Centers" anchor (`#delivery-centers`) collided with the dashboard's own Delivery Centers table route, since `APP_SHELL_HASHES` also treats that hash as an app-shell hash — clicking it (in both Header and Hero) silently entered the dashboard instead of scrolling to the marketing section. Renamed the marketing anchor to `#our-delivery-centers`. Gate: 1 FAIL (test-writer) + 1 Critical (pr-test-analyzer), both the same finding (no regression test on the collision fix) confirmed by 4 agents total — fixed with tests asserting the anchor is absent from `APP_SHELL_HASHES`, guarding the whole class of bug. 32/32 frontend + 50/50 backend tests pass. |
| FEAT-2 | completed | Rework the AppShell sidebar/dashboard from FEAT-1: list each master-data table as its own separate sidebar item instead of one generic entry; clicking an item shows that table's data on the right. Remove "Master Data" from user-facing UI text. Top page header reads "Dashboard". | claude/trusting-curie-hlx1r6 | Built directly (owner chose not to run the full dev-team pipeline for this well-scoped, concrete change). `NAV_ITEMS` is now derived straight from `MASTER_DATA_TABLES` (one nav item per table, carrying `route`/`columns`) instead of holding a single generic entry with an inner tab-bar view — removed the now-unused `MasterDataView` component. `AppShell` renders `MasterDataTable` directly per selected item; `TopBar` shows a static "Dashboard" heading (sidebar's active-item state carries which section is selected). Marketing `Header`'s nav link relabeled "Dashboard", pointing at the first table's hash. 26/26 frontend tests pass, 50/50 backend tests pass, clean build/lint, self-reviewed via `/code-review` (no findings). |
| FEAT-1 | completed | Redesign the frontend UI so it reads as a multi-user internal application (dashboard/app shell — nav, workspace chrome, data-dense views) rather than a public-facing "portal" landing-page style. | claude/trusting-curie-hlx1r6 | Round-1 architecture-critic halt (AC-1: AppShell wrapped in old marketing Header/Footer) resolved by solution-architect's round-2 SDD amendment: App.jsx now uses a single outermost `APP_SHELL_HASHES.includes(hash)` ternary so AppShell and the marketing Header/main/Footer tree never co-mount. New: AppShell/Sidebar/TopBar/UserBadge components, src/lib/navigation.js, src/lib/currentUser.js (mock, explicitly labeled — no real auth backend exists), src/hooks/useCurrentUser.js. MasterDataTable re-parented as a child view inside AppShell. Final enterprise-architect-post sign-off: PASS — clean `oxlint`, clean `npm run build` (58 modules), 25/25 tests passing, no stubs/TODOs/secrets, no unresolved findings. Ready for CLAUDE.md §9.1 auto-merge-to-main. |
